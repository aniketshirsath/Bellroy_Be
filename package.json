{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/json-server",
    "start": "json-server --host 0.0.0.0 --port $PORT --watch new.json"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
